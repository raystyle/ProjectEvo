# 2026-09-08 bh 与 reader 使用过程

## 概貌

从测试 browser/reader 技能，推到 Google-Medium-抓取研究，再推到 `E:\研究资料` 电子书抽样。用户裁定工位复用。过程技巧落 `docs\research\S001`，实证抽进 skill 的 tool-browser-harness 与 tool-reader。

## 关键裁决

只开 1 到 2 个 tab 复用，不再新开附着；Chrome Allow 不由 agent 代点。herdr 二进制先不升级（同日更早裁定）。

## 得

附着一次、HTTP 优先、pluck 对现场、坏 EPUB 跳过、扫描页 OCR、大盘禁递归。

## 失与坑

`--new-tab` 开过 example.org 关不掉；`bh --restart` 为自愈反而再要 Allow；目录递归列举卡死两次。

## 明日入口

S001 待办：`技术资料\` 子树、Data Science EPUB 空壳、残留 tab。
